export class User {
    userId:number;
    emailId:string;
    userName:string;
    password:string;

    constructor()
    {}

    // constructor(userId:number, emailId:string, userName:string, password:string)
    // {
    //     this.userId = userId;
    //     this.emailId = emailId;
    //     this.userName = userName;
    //     this.password = password;
    // }
}
